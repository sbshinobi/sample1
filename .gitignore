# Node modules directory (contains all dependencies installed by npm/yarn)
node_modules/

# Build output directories (created after running `npm run build` or `yarn build`)
build/
dist/

# .env files (store sensitive information like API keys)
.env
.env.local
.env.*.local

# Log files (generated during npm/yarn operations)
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE/editor configuration files for VS Code
.vscode/

# IDE/editor configuration files for other editors (if applicable)
.idea/
*.sublime-workspace
*.sublime-project

# TypeScript build info (if using TypeScript in your React project)
*.tsbuildinfo

# Cache files (generated by React, Babel, Webpack, etc.)
.cache/

# Coverage reports (generated by Jest or other testing tools)
coverage/

# OS-specific files (macOS, Windows)
.DS_Store          # macOS
Thumbs.db          # Windows

# Optional: Docker files (if using Docker for your React project)
docker-compose.override.yml
.docker/

# Optional: Jest-specific files for styled-components testing
jest-styled-components/

# Lock files (if you use yarn, ignore package-lock.json and vice versa)
# If using Yarn, ignore package-lock.json
yarn.lock
package-lock.json

# Optional: Ignore the VS Code workspace settings if shared folders might contain them
.vscode/*
